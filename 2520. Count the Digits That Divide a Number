class Solution:
    def countDigits(self, num: int) -> int:
        count = 0
    # Convert to string to easily iterate over each digit character
        for char in str(num):
            digit = int(char)
        # Check if the original number is divisible by the digit
            if num % digit == 0:
                count += 1
            
        return count
